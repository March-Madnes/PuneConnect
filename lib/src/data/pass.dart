import 'package:flutter/material.dart';

class Pass {
  final int passIndex;
  final String title;
  final int price;
  final Icon passIcon;

  Pass(this.passIndex, this.title, this.price, this.passIcon);
}